#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
osascript -e "tell application \"Terminal\" to do script \"echo 'File name:'; read FILENAME; OUTPUT_DIR=$1 $DIR/dropscan_mac \$FILENAME\""

